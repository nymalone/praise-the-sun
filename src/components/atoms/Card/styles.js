import styled from 'styled-components/native';

export const CardContainer = styled.View`
  padding: 10px;
  background: rgba(200,255,255,0.28);
  border-radius: 20;
  height: auto;
  margin: 20px 0 5px 0;
  position: relative;
`;
